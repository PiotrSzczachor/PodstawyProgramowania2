#include <iostream>
#include <string>
#include <fstream>
#include <ctime>
#include <vector>
#include "Logger.h"
#include "ConsoleLogger.h"
#include "FileLogger.h"

using namespace std;

// TODO:

int main(int argc, char* argv[])
{/*
    Logger::loggers.push_back(make_pair("Logger", Logger::instance()));
    Logger::loggers.push_back(make_pair("ConsoleLogger", ConsoleLogger::instance()));
    Logger::loggers.push_back(make_pair("FileLogger", FileLogger::instance()));
     ... i inne loggery
    if (argc > 1)
    {
        Logger::setInstanceByName(argv[1]);
    }
    Logger::instance().log("Meldujemy wykonanie zadania!");
     */

}
